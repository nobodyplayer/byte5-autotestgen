class TestCasePrompts:
    
    @staticmethod
    def get_image_categorization_prompt(image_count: int) -> str:
        """生成用于图片分类的Prompt"""
        return f"""
        请对以下{image_count}张图片进行分类，识别出哪些是【流程图】、哪些是【功能结构图】、哪些是【UI界面图】。
        请严格按照以下格式返回每种分类包含的图片索引（从0开始），如果某个分类下没有图片，则返回空列表。

        流程图: [索引1, 索引2, ...]
        功能结构图: [索引A, 索引B, ...]
        UI界面图: [索引X, 索引Y, ...]
        """

    @staticmethod
    def get_image_analysis_prompt(category: str, prd_text: str = "") -> str:

    # 通用开头，声明角色与要求
        common_intro = (
            "System:\n"
            "你是一名资深业务分析师和测试架构师，擅长从多模态输入中提取可执行的结构化信息。"
            "所有输出必须严格遵循 JSON 格式，不得包含任何解释性文字或注释。\n\n"
        )

        # 针对不同类别的模板
        templates = {
            "flowchart": (
                "请分析这张业务流程图：\n"
                "输出一个 JSON 对象，包含键 nodes 和 edges。\n"
                "  - nodes: 包含所有业务节点名称的数组。\n"
                "  - edges: 包含所有有向边的数组，每项格式 {\"from\": 起点, \"to\": 终点, \"condition\": 判断条件或空字符串}。\n"
                "示例：\n"
                "{\n"
                "  \"nodes\": [\"登录页面\",\"输入凭据\",\"验证成功\",\"验证失败\"],\n"
                "  \"edges\": [\n"
                "    {\"from\":\"登录页面\",\"to\":\"输入凭据\",\"condition\":\"\"},\n"
                "    {\"from\":\"输入凭据\",\"to\":\"验证成功\",\"condition\":\"账号密码正确\"},\n"
                "    {\"from\":\"输入凭据\",\"to\":\"验证失败\",\"condition\":\"账号或密码错误\"}\n"
                "  ]\n"
                "}\n"
            ),
            "structure": (
                "请解析这张功能结构图：\n"
                "输出一个 JSON 对象，包含键 modules，每个模块包含 name 和可选的 submodules。\n"
                "示例：\n"
                "{\n"
                "  \"modules\": [\n"
                "    {\"name\":\"用户管理\",\"submodules\":[{\"name\":\"登录\"},{\"name\":\"注册\"}]},\n"
                "    {\"name\":\"订单管理\",\"submodules\":[{\"name\":\"创建订单\"},{\"name\":\"查询订单\"}]}\n"
                "  ]\n"
                "}\n"
            ),
            "ui": (
                "请提取这张 UI 界面截图中的所有可交互控件：\n"
                "输出一个 JSON 数组，每个元素包含字段：\n"
                "  - name: 控件显示文本\n"
                "  - type: 控件类型，限于 button、input、select、link、checkbox\n"
                "  - identifier: 唯一标识符（如 id 或 aria-label）\n"
                "  - module: 控件所属功能模块名称（如“登录”“找回密码”等），如不清晰可留空字符串\n"
                "示例：\n"
                "[\n"
                "  {\"name\":\"用户名输入框\",\"type\":\"input\",\"identifier\":\"#username\",\"module\":\"登录\"},\n"
                "  {\"name\":\"登录按钮\",\"type\":\"button\",\"identifier\":\"#loginBtn\",\"module\":\"登录\"}\n"
                "]\n"
            ),
        }

        # 选择模板
        prompt_body = templates.get(category, (
            "请描述该图片的主要内容，输出纯 JSON，不要其他任何文字。\n"
        ))

        # 如果有 PRD 文本上下文，附加提示
        if prd_text:
            prompt_body += (
                "\n\n请结合以下 PRD 文本，只提取与此图片相关的业务信息：\n"
                f"{prd_text.strip()}\n"
            )

        return common_intro + prompt_body



    @staticmethod
    def get_final_test_points_prompt(
        prd_text: str, 
        flowchart_info: str, 
        structure_info: str, 
        ui_info: str, 
        context: str, 
        requirements: str
    ) -> str:
        """生成最终的测试点生成Prompt"""
        return f"""
        # 角色
        你是一名顶级的软件测试专家，精通从多种需求信息中提炼测试要点。

        # 任务
        你的任务是根据下面提供的所有信息，设计一套全面、深入的功能测试用例，并严格按照指定的JSON格式输出。

        # 可用信息
        1.  **PRD核心文本**：
            ```
            {prd_text or '无'}
            ```
        2.  **流程图分析结果**：
            ```
            {flowchart_info or '无'}
            ```
        3.  **功能结构图分析结果**：
            ```
            {structure_info or '无'}
            ```
        4.  **UI界面图分析结果**：
            ```
            {ui_info or '无'}
            ```
        5.  **额外上下文**：
            ```
            {context or '无'}
            ```
        6.  **特殊要求**：
            ```
            {requirements or '无'}
            ```

        # 输出格式要求
        - 从上述所有输入中**自动识别**所有功能模块名称（如“用户登录”、“找回密码”等）。
        - 对每个功能模块，提取**完整的测试点列表**：包括正向流程、边界条件、异常场景和负向测试。
        - 每条测试点必须以“验证”开头，保持为一行完整的句子，不得换行或拆分。
        - 最终输出格式：一个 JSON 对象，键为功能模块名称，值为该模块测试点的字符串数组。

        # 输出示例
        ```json
        {{
            "用户登录与认证": [
                "验证使用有效用户名和密码可以成功登录",
                "验证使用无效密码登录时，系统会返回指定的错误提示",
                "测试密码输入框的明文/密文切换功能",
                "测试账户因多次密码错误而被锁定的场景"
            ],
            "个人资料修改": [
                "验证用户可以成功修改自己的昵称",
                "验证昵称长度超过系统上限时，会提示错误"
            ]
        }}
        ```
        """
    

   
class SystemMessages:
    """系统消息模板"""    
    
    MULTIMODAL_ANALYSIS = (
        "你是一个专业的测试工程师，专门负责基于多模态PRD文档（包含文本和图片）生成高质量的测试用例。"
        "请综合分析PRD文档的文本内容和相关图片（如UI设计图、流程图、架构图等），"
        "深入理解产品需求、用户交互流程和业务逻辑，"
        "生成全面、准确且可执行的测试用例。"
        "特别注意图片中的UI元素、交互细节和业务流程，"
        "确保测试用例覆盖所有功能点、用户场景、正常流程、异常情况和边界条件。"
    )

class ImagePrompts:
    """图片处理相关的提示词模板"""
    
    # 图片分类提示词
    CLASSIFICATION = """
你是一个专业的图片分析师。请分析这张图片，判断它属于以下哪种类型：

1. **UI图** - 包含用户界面设计、页面布局、按钮、表单、菜单等UI元素的图片
2. **流程图** - 包含业务流程、用户操作流程、系统流程等的流程图
3. **框架图** - 包含系统架构、技术架构、模块关系等的架构图

请仔细观察图片内容，只需要回答图片类型，格式如下：
图片类型：[UI图/流程图/框架图]
"""
    
    # UI图提取提示词
    UI_EXTRACTION = """
你是一个专业的UI分析师。请仔细分析这张UI设计图，提取以下信息：

1. **页面标题/功能名称**：这个页面的主要功能是什么
2. **UI元素列表**：
   - 按钮（名称、位置、功能）
   - 输入框（标签、类型、验证规则）
   - 下拉框/选择器（选项内容）
   - 文本显示区域
   - 图标和链接
   - 其他交互元素
3. **页面布局**：元素的排列和层次关系
4. **交互逻辑**：用户可能的操作路径和交互方式
5. **状态变化**：不同状态下UI的变化（如加载、成功、错误状态）

请以结构化的方式输出，便于后续测试用例生成时使用。
"""
    
    # 流程图提取提示词
    FLOWCHART_EXTRACTION = """
你是一个专业的流程分析师。请仔细分析这张流程图，提取以下信息：

1. **流程名称**：这个流程的主要目的和功能
2. **流程节点**：
   - 起始节点：流程的开始点
   - 处理节点：具体的操作步骤
   - 判断节点：需要做决策的分支点
   - 结束节点：流程的结束点
3. **节点关系**：
   - 节点之间的连接关系
   - 分支条件和路径
   - 循环和跳转逻辑
4. **异常处理**：错误情况的处理路径
5. **角色和权限**：不同角色在流程中的参与点

请以结构化的方式输出，包含完整的流程路径和分支逻辑。
"""
    
    # 框架图提取提示词
    FRAMEWORK_EXTRACTION = """
你是一个专业的功能架构图分析师。请分析这张功能架构图，提取以下信息：

1. **功能模块**：
   - 主要功能模块和组件
   - 各模块的核心功能
   - 模块之间的功能依赖关系

2. **功能流程**：
   - 主要业务流程
   - 功能调用链路
   - 数据处理流程

3. **功能边界**：
   - 各模块的功能边界
   - 对外接口和服务
   - 功能集成点

请以结构化的方式输出分析结果，重点关注各功能模块的职责和交互，为功能测试用例设计提供依据。
"""

class ErrorMessages:
    """错误消息模板"""
    
    FEISHU_SERVICE_NOT_INITIALIZED = "飞书服务未初始化，请提供飞书应用凭证"
    DOCUMENT_CONTENT_EMPTY = "无法获取文档内容或文档为空"
    
    @staticmethod
    def get_feishu_error(error_detail: str) -> str:
        """获取飞书相关错误消息"""
        return f"获取飞书文档内容失败: {error_detail}"
    
    @staticmethod
    def get_generation_error(error_detail: str) -> str:
        """获取测试用例生成错误消息"""
        return f"生成测试用例时出错: {error_detail}"
